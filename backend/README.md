To install dependencies:

```sh
bun install
```

To run:

```sh
bun run dev
```

To update database schema:

```bash
bun run db:generate
```

```bash
bun run db:migrate
```

open http://localhost:3000

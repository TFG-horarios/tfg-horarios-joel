version: "1.0"
linter: jetbrains/qodana-js:latest

bootstrap: |
  curl -fsSL https://bun.sh/install | bash && \
  /root/.bun/bin/bun install

exclude:
  - name: All
    paths:
      - "**/node_modules"
      - "**/dist"
      - "**/.next"